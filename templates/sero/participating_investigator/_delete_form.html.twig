<form method="post" action="{{ path('app_s_e_r_o_participating_investigator_delete', {'id': participating_investigator.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ participating_investigator.id) }}">
    <button class="btn">Delete</button>
</form>
