{% extends 'base.html.twig' %}

{% block title %}ReviewType{% endblock %}

{% block body %}
    <h1>ReviewType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ review_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ review_type.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ review_type.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_s_e_r_o_review_type_index') }}">back to list</a>

    <a href="{{ path('app_s_e_r_o_review_type_edit', {'id': review_type.id}) }}">edit</a>

    {{ include('sero/review_type/_delete_form.html.twig') }}
{% endblock %}
