<!-- Button trigger modal-->
<button type="button" class="btn btn-primary m-4" data-toggle="modal" data-target="#contuoationModal{{version.id}}">
	 Continuation request
</button>

<!-- Modal-->
<div class="modal fade" id="contuoationModal{{version.id}}" tabindex="-1" role="dialog" aria-labelledby="contuoationModalLabel" aria-hidden="true">
	<div class="modal-dialog" role="document">
		<div class="modal-content">
			<div class="modal-header">
				<h5 class="modal-title" id="contuoationModalLabel">{% trans %}Continuation request{% endtrans %}</h5>
				<button type="button" class="close" data-dismiss="modal" aria-label="Close">
					<i aria-hidden="true" class="ki ki-close"></i>
				</button>
			</div>
			<div class="modal-body">
				{# <form method="post" action="{{ path('new_contuoation', {'id': application.id}) }}" >
					 
					<div class="modal-footer">
						<button type="submit" class="btn btn-light-primary font-weight-bold" data-dismiss="modal">Close</button>
						<button class="btn btn-primary font-weight-bold">{{ button_label|default('Save') }}</button>
					</div>
				</form> #}
			</div>
		</div>
	</div>
</div>
