{% extends 'base.html.twig' %}

{% block title %}BoardMember{% endblock %}

{% block body %}
    <h1>BoardMember</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ board_member.id }}</td>
            </tr>
            <tr>
                <th>AssignedAt</th>
                <td>{{ board_member.assignedAt ? board_member.assignedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ board_member.status }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ board_member.role }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_s_e_r_o_board_member_index') }}">back to list</a>

    <a href="{{ path('app_s_e_r_o_board_member_edit', {'id': board_member.id}) }}">edit</a>

    {{ include('sero/board_member/_delete_form.html.twig') }}
{% endblock %}
