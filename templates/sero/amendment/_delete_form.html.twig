<form method="post" action="{{ path('app_s_e_r_o_amendment_delete', {'id': amendment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ amendment.id) }}">
    <button class="btn">Delete</button>
</form>
