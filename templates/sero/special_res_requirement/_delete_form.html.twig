<form method="post" action="{{ path('app_s_e_r_o_special_res_requirement_delete', {'id': special_res_requirement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ special_res_requirement.id) }}">
    <button class="btn">Delete</button>
</form>
