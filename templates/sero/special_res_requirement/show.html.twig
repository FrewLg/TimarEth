{% extends 'base.html.twig' %}

{% block title %}SpecialResRequirement{% endblock %}

{% block body %}
    <h1>SpecialResRequirement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ special_res_requirement.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ special_res_requirement.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_s_e_r_o_special_res_requirement_index') }}">back to list</a>

    <a href="{{ path('app_s_e_r_o_special_res_requirement_edit', {'id': special_res_requirement.id}) }}">edit</a>

    {{ include('sero/special_res_requirement/_delete_form.html.twig') }}
{% endblock %}
