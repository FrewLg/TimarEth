<form method="post" action="{{ path('reviewers_pool_delete', {'id': reviewers_pool.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reviewers_pool.id) }}">
    <button class="btn">Delete</button>
</form>
