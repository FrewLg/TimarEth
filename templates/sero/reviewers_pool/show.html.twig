{% extends 'base.html.twig' %}

{% block title %}ReviewersPool{% endblock %}

{% block body %}
    <h1>ReviewersPool</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reviewers_pool.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ reviewers_pool.name }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ reviewers_pool.email }}</td>
            </tr>
            <tr>
                <th>Affiliation</th>
                <td>{{ reviewers_pool.affiliation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_s_e_r_o_reviewers_pool_index') }}">back to list</a>

    <a href="{{ path('app_s_e_r_o_reviewers_pool_edit', {'id': reviewers_pool.id}) }}">edit</a>

    {{ include('sero/reviewers_pool/_delete_form.html.twig') }}
{% endblock %}
