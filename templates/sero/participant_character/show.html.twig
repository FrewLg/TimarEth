{% extends 'base.html.twig' %}

{% block title %}ParticipantCharacter{% endblock %}

{% block body %}
    <h1>ParticipantCharacter</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ participant_character.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ participant_character.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_s_e_r_o_participant_character_index') }}">back to list</a>

    <a href="{{ path('app_s_e_r_o_participant_character_edit', {'id': participant_character.id}) }}">edit</a>

    {{ include('sero/participant_character/_delete_form.html.twig') }}
{% endblock %}
