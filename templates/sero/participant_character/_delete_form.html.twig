<form method="post" action="{{ path('app_s_e_r_o_participant_character_delete', {'id': participant_character.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ participant_character.id) }}">
    <button class="btn">Delete</button>
</form>
