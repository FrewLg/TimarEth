{% extends 'base.html.twig' %}

{% block title %}ProceedureUse{% endblock %}

{% block body %}
    <h1>ProceedureUse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ proceedure_use.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ proceedure_use.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_s_e_r_o_proceedure_use_index') }}">back to list</a>

    <a href="{{ path('app_s_e_r_o_proceedure_use_edit', {'id': proceedure_use.id}) }}">edit</a>

    {{ include('sero/proceedure_use/_delete_form.html.twig') }}
{% endblock %}
