{% extends 'base.html.twig' %}

{% block title %}ReviewStatus{% endblock %}

{% block body %}
    <h1>ReviewStatus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ review_status.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ review_status.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ review_status.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_s_e_r_o_review_status_index') }}">back to list</a>

    <a href="{{ path('app_s_e_r_o_review_status_edit', {'id': review_status.id}) }}">edit</a>

    {{ include('sero/review_status/_delete_form.html.twig') }}
{% endblock %}
