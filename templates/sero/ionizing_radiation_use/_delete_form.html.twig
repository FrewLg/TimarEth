<form method="post" action="{{ path('app_s_e_r_o_ionizing_radiation_use_delete', {'id': ionizing_radiation_use.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ionizing_radiation_use.id) }}">
    <button class="btn">Delete</button>
</form>
