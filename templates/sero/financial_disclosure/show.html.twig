{% extends 'base.html.twig' %}

{% block title %}FinancialDisclosure{% endblock %}

{% block body %}
    <h1>FinancialDisclosure</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ financial_disclosure.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ financial_disclosure.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_s_e_r_o_financial_disclosure_index') }}">back to list</a>

    <a href="{{ path('app_s_e_r_o_financial_disclosure_edit', {'id': financial_disclosure.id}) }}">edit</a>

    {{ include('sero/financial_disclosure/_delete_form.html.twig') }}
{% endblock %}
