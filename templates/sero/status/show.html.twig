{% extends 'base.html.twig' %}

{% block title %}Status{% endblock %}

{% block body %}
    <h1>Status</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ status.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ status.name }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ status.code }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ status.color }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_s_e_r_o_status_index') }}">back to list</a>

    <a href="{{ path('app_s_e_r_o_status_edit', {'id': status.id}) }}">edit</a>

 {% endblock %}
