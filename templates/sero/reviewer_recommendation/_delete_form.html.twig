<form method="post" action="{{ path('app_s_e_r_o_reviewer_recommendation_delete', {'id': reviewer_recommendation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reviewer_recommendation.id) }}">
    <button class="btn">Delete</button>
</form>
