{% extends 'base.html.twig' %}

{% block title %}ApplicationType{% endblock %}

{% block body %}
    <h1>ApplicationType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ application_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ application_type.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ application_type.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_s_e_r_o_application_type_index') }}">back to list</a>

    <a href="{{ path('app_s_e_r_o_application_type_edit', {'id': application_type.id}) }}">edit</a>

    {{ include('sero/application_type/_delete_form.html.twig') }}
{% endblock %}
