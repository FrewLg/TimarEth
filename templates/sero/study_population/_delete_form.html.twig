<form method="post" action="{{ path('app_s_e_r_o_study_population_delete', {'id': study_population.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ study_population.id) }}">
    <button class="btn">Delete</button>
</form>
