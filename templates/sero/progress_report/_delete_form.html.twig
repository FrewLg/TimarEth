<form method="post" action="{{ path('app_s_e_r_o_progress_report_delete', {'id': progress_report.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ progress_report.id) }}">
    <button class="btn">Delete</button>
</form>
