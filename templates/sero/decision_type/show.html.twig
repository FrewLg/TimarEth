{% extends 'base.html.twig' %}

{% block title %}DecisionType{% endblock %}

{% block body %}
    <h1>DecisionType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ decision_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ decision_type.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ decision_type.description }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ decision_type.createdAt ? decision_type.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_s_e_r_o_decision_type_index') }}">back to list</a>

    <a href="{{ path('app_s_e_r_o_decision_type_edit', {'id': decision_type.id}) }}">edit</a>

    {{ include('sero/decision_type/_delete_form.html.twig') }}
{% endblock %}
