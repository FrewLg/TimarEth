<form method="post" action="{{ path('app_s_e_r_o_decision_type_delete', {'id': decision_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ decision_type.id) }}">
    <button class="btn">Delete</button>
</form>
