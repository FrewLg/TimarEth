{% extends 'base.html.twig' %}

{% block title %}Edit Compliant{% endblock %}

{% block body %}
    <h1>Edit Compliant</h1>

    {{ include('compliant/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_compliant_index') }}">back to list</a>

    {{ include('compliant/_delete_form.html.twig') }}
{% endblock %}
